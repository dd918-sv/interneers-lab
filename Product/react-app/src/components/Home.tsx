import React from "react";
import { useState } from "react";

const HelloMessage = () => {
  return (
    <div className="hello-message"style={{position:"absolute",top:'15rem',left:'40rem'}}>
      <h1>Welcome to Products Inventory</h1>
    </div>
  );
};

export default HelloMessage;
